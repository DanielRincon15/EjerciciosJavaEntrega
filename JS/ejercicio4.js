function usrpas(){
    if (document.form1.txt.value=="administrador" && document.form1.num.value=="123456789"){window.location="home.htm"}
    else {alert("Error en Usuario o Contraseña. Intenta de nuevo.")}
    }
